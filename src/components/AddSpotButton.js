import React from "react";

class AddSpotButton extends React.Component {
  render() {
    return (
      <button>
        <span>Add Spot</span>
      </button>
    );
  }
}

export default AddSpotButton;